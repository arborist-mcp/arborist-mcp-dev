{
  "suites": {
    "check-workflow": {
      "module": "tests.test_check_workflow",
      "description": "Run check-profile manifest and CI workflow contract tests.",
      "requires_extension": false
    },
    "test-workflow": {
      "module": "tests.test_test_workflow",
      "description": "Run Python test-suite manifest and test.ps1 contract tests.",
      "requires_extension": false
    },
    "benchmark-workflow": {
      "module": "tests.test_benchmark_workflow",
      "description": "Run benchmark helper parsing and threshold contract tests.",
      "requires_extension": false
    },
    "index-watch": {
      "module": "tests.test_index_watch",
      "description": "Run pure-Python index watch reconciliation and CLI tests.",
      "requires_extension": false
    },
    "index-watch-native": {
      "module": "tests.test_index_watch_native",
      "description": "Run index watch --once tests against the synced native extension.",
      "requires_extension": true
    }
  },
  "groups": {
    "python-fast": {
      "description": "Run fast pure-Python workflow tests plus the gateway-fast protocol suites.",
      "entries": [
        "check-workflow",
        "test-workflow",
        "benchmark-workflow",
        "index-watch",
        "gateway-fast"
      ]
    },
    "python-native": {
      "description": "Run the Python suites that execute against the synced native extension.",
      "entries": [
        "index-watch-native",
        "gateway-native"
      ]
    },
    "python": {
      "description": "Run the full Python unittest suite set.",
      "entries": [
        "check-workflow",
        "test-workflow",
        "benchmark-workflow",
        "index-watch",
        "index-watch-native",
        "gateway"
      ]
    }
  }
}
