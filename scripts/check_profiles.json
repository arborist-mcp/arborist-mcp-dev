{
  "profiles": {
    "sanity": {
      "description": "Run PowerShell syntax, version consistency, and tool catalog snapshot checks.",
      "handler": "sanity"
    },
    "rust": {
      "description": "Run cargo fmt --check, cargo test --locked, and cargo clippy --locked --all-targets -D warnings.",
      "handler": "rust"
    },
    "gateway-fast": {
      "description": "Run the pure-Python gateway protocol suites that do not require the synced native extension.",
      "handler": "suite",
      "suite": "gateway-fast",
      "sync_extension": "never",
      "prepare_extension": false
    },
    "python-fast": {
      "description": "Run the fast pure-Python workflow and gateway suite group without syncing the native extension.",
      "handler": "suite",
      "suite": "python-fast",
      "sync_extension": "never",
      "prepare_extension": false
    },
    "gateway-native": {
      "description": "Build and sync the native gateway extension, then run the gateway-native protocol suites.",
      "handler": "suite",
      "suite": "gateway-native",
      "sync_extension": "never",
      "prepare_extension": true
    },
    "python-discovery": {
      "description": "Build and sync the native gateway extension, then run the full manifest-defined Python unittest suite.",
      "handler": "suite",
      "suite": "python",
      "sync_extension": "never",
      "prepare_extension": true
    },
    "gateway-smoke": {
      "description": "Build and sync the native gateway extension, then exercise gateway CLI/catalog/tools/initialize smoke checks.",
      "handler": "gateway-smoke",
      "prepare_extension": true
    },
    "python-native": {
      "description": "Run the aggregate native Python gate: gateway-native, python-discovery, and gateway-smoke.",
      "entries": [
        "gateway-native",
        "python-discovery",
        "gateway-smoke"
      ]
    },
    "full": {
      "description": "Run the complete local check gate: sanity, rust, and the aggregate python-native profile.",
      "entries": [
        "sanity",
        "rust",
        "python-native"
      ]
    }
  },
  "ci_profiles": [
    "sanity",
    "rust",
    "python-fast",
    "gateway-native",
    "python-discovery",
    "gateway-smoke"
  ]
}
